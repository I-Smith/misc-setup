export JAVA_HOME=$(/usr/libexec/java_home)
export AWS_MFA_ARN=
export GOPRIVATE=
export AWS_PROFILE=
# GMAIL_APP_PASSWORD lives in macOS Keychain, not here.
# New machine setup: security add-generic-password -a "$USER" -s "gmail-app-password" -w "your-app-password"
